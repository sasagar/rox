{
  "name": "rox",
  "version": "2025.11.0-alpha.1",
  "private": true,
  "description": "Lightweight ActivityPub server & client with Misskey API compatibility",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "start": "bun scripts/start-production.ts",
    "test": "bun test",
    "lint": "oxlint",
    "format": "oxlint --fix",
    "typecheck": "bun run --filter '*' typecheck",
    "backend:dev": "bun run --filter backend dev",
    "frontend:dev": "bun run --filter frontend dev",
    "backend:start": "bun run --cwd packages/backend src/index.ts",
    "frontend:start": "bun run --cwd packages/frontend start",
    "db:generate": "bun run --filter backend db:generate",
    "db:migrate": "bun run --filter backend db:migrate",
    "db:studio": "bun run --filter backend db:studio",
    "typedoc": "typedoc"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.2",
    "oxlint": "^0.15.15",
    "typedoc": "^0.28.14",
    "typescript": "^5.7.2"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}